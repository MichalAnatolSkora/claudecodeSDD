# AGENTS.md

This repo keeps its working conventions in **[CLAUDE.md](CLAUDE.md)** — read that file.

`AGENTS.md` is the cross-tool convention name some agents look for; this is a thin pointer so they land in the same place. Everything that matters — editorial structure, the neutral-naming rules, the PDF build, the working style — lives in `CLAUDE.md`. It is deliberately **not** copied here: a second copy only drifts (this file was previously a stale clone that pointed at guides which don't exist).
